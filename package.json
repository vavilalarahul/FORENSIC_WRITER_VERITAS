{
  "name": "forensic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix forensic-writer",
    "install-all": "npm run install-all --prefix forensic-writer",
    "server": "npm run server --prefix forensic-writer",
    "client": "npm run client --prefix forensic-writer"
  }
}
